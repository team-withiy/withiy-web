"use client";

import type { ReactNode } from "react";

type Size = 52;

interface Props {
  children: ReactNode;
  size: Size;
  disabled?: boolean;
}

const Select: React.FC<Props> = () => {
  return null;
};

export default Select;
